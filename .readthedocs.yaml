version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "3.14"

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs

formats:
  - pdf
  - epub
